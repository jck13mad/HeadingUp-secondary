
<div class="item_page">
    <h2 class="page_title">Viewing Item</h2>
    <br>
    
    <% h = Head.find(params[:head_id]) %>

    <h3 class="item_name"><%= h.brand %> <%= h.name%></h3>
    <img class="item_image" src='/img/<%= h.img %>'>
    <div class="desc_box"
        <p class="item description"><%= h[:description] %></p>
    </div>
</div>
